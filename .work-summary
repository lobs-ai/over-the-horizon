Implemented zoom gestures + distance range control. Fixed ContentView gesture handling to calculate incremental scale deltas (MagnificationGesture reports cumulative values). Zoom OUT increases min/max distances with smaller labels; zoom IN decreases distances with larger labels. Added 14 new integration tests for incremental gesture handling. All tests passing.
